%% define the main template style for the document
\documentclass{article}

%\usepackage{nips_2017}
% to compile a camera-ready version, add the [final] option, e.g.:
\usepackage[final]{nips_2017}

%% Import packages with parameters
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[pagebackref=true]{hyperref}
%% Import general packages
\usepackage{
  amsmath, amssymb, amsfonts,
  algorithmic, textcomp, listings,
  graphicx, subfig,
  booktabs, longtable,
  url, nicefrac, microtype
}

% override tightlist. error with pandoc?
\def\tightlist{}

%% start the document, the Markdown parser takes over from this point
\begin{document}
% pandoc will inject the parsed Markdown right here
$body$
% output everything in the reference file, even if it wasn't referenced in the
% paper. This is a proposal and should enumerate our potential sources.
\nocite{*}
% genererate the references
{\small
	\bibliographystyle{my-unsrtnat}
	\bibliography{references}
}
\end{document}
